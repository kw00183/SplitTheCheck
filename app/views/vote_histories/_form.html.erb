<%= form_with(model: vote_history) do |form| %>
  <% if vote_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vote_history.errors.count, "error") %> prohibited this vote_history from being saved:</h2>

      <ul>
        <% vote_history.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :restaurant_id %>
    <%= form.number_field :restaurant_id %>
  </div>

  <div class="field">
    <%= form.label :vote_split %>
    <%= form.check_box :vote_split %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
